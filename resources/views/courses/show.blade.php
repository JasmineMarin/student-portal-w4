<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ $code }}</title>
</head>
<body>
    <h1>{{ $code }}: {{ $title }}</h1>
    <a href="{{ route('courses.index') }}">← Back to Courses</a>
</body>
</html>